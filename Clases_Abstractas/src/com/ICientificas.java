package com;

//Todos los metodos dentro de una interface son 100% abstractos
//Son publicos por default
//@FunctionalInterface (solo tiene un metodo abstracto)
public interface ICientificas {
	
	double raizCuadrada(double num);
	double seno(double num);

}
